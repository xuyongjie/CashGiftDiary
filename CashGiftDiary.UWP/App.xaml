<Application
    x:Class="CashGiftDiary.UWP.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CashGiftDiary.UWP"
    RequestedTheme="Light">
    <Application.Resources>
        <Style TargetType="Button" x:Key="ButtonDefault">
            <Setter Property="Width" Value="80"/>
            <Setter Property="Margin" Value="4"/>
        </Style>
        <Style TargetType="TextBox" x:Key="TextBoxDefault">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="{x:Null}"/>
        </Style>
    </Application.Resources>

</Application>
